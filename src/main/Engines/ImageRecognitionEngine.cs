namespace DAMBackend.ImageRecognitionEngine
{
    // looks like we may implement this if we done MVP
    public class Division // placeholder
    {
        private int _a;
        private int _b;
        public Division(int a, int b) {
        }
        public int Div(int a, int b)
        {
            return a / b;
        }
    }
}